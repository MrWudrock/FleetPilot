interface:
  display_name: "FleetPilot"
  short_description: "Repo-specific patterns and workflows for FleetPilot"
  default_prompt: "Use the FleetPilot repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true